"use client";

import AdminShell from '@/components/admin/AdminShell';

export default function TestAttendanceAllLayout({ children }: { children: React.ReactNode }) {
  return <AdminShell>{children}</AdminShell>;
}
